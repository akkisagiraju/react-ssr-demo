import React from 'react';

const Home = () => {
  return (
    <div>
      <h1>React SSR app</h1>
      <button onClick={() => console.log('hello')}>Say hello!</button>
    </div>
  );
};

export default Home;
